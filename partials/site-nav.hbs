<nav class="site-nav">
  <div class="site-nav-left">
    <a href="{{@site.url}}">
      {{#if logoImage}}
      <img class="site-nav__logo" src="{{asset logoImage}}" />
      {{else}}
      <img class="site-nav__logo" src="{{asset "img/mrb-logo.svg"}}" />
      {{/if}}
    </a>
  </div>
  <div class="site-nav-right">
    {{#if @site.navigation}} {{navigation}} {{/if}}
  </div>
</nav>
